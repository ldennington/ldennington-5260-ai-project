namespace TradeGame
{
    internal class ScheduleGenerator
    {
        //public IList<Country> GenerateSuccessors(IList<Country> initialState)
        //{
        //    IList<Country> successors = new List<Country>();

        //    foreach(TransformTemplate template in Database.TransformTemplates)
        //    {
        //        foreach (Country country in initialState)
        //        {
        //            if (country.IsSelf)
        //            {
        //                int multiplier = 1;
        //                IDictionary<string, bool> resourcesAndPreconditions = new Dictionary<string, bool>();
        //                foreach (string resource in template.Inputs.Keys)
        //                {
        //                    if (template.Inputs[resource] <= country.State[resource]*multiplier)
        //                    {
        //                        resourcesAndPreconditions.Add(resource, true);
        //                    }
        //                    else
        //                    {
        //                        resourcesAndPreconditions.Add(resource, false);
        //                    }
        //                }

        //                if (resourcesAndPreconditions.Count == 0)
        //                {

        //                }
        //            }
        //        }
        //    }
        //}
    }
}
